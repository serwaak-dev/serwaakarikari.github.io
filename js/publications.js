const list=document.querySelector('#publication-list');if(list){fetch('data/publications.json').then(r=>r.json()).then(data=>{const search=document.querySelector('#pub-search'),filter=document.querySelector('#pub-filter');const render=()=>{const q=search.value.toLowerCase(),f=filter.value;const rows=data.filter(p=>(!f||p.topic===f)&&Object.values(p).join(' ').toLowerCase().includes(q));list.innerHTML=rows.map(p=>`<article class="publication"><div class="meta">${p.year} · ${p.type} · ${p.topic}</div><h3>${p.title}</h3><p>${p.authors}</p><p><em>${p.journal}</em>${p.doi?` · <a href="${p.doi}" target="_blank" rel="noopener">DOI</a>`:''}</p></article>`).join('')||'<p>No publications match your search.</p>'};[search,filter].forEach(el=>el.addEventListener('input',render));render()})}
